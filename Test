-- Multi-Fruit Fake Gifter (ALL 38 Fruits)
local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- ALL Permanent Fruits List
local fruits = {"Rocket", "Spin", "Chop", "Spring", "Bomb", "Smoke", "Spike", "Flame", "Falcon", "Ice", "Sand", "Dark", "Diamond", "Light", "Rubber", "Barrier", "Magma", "Quake", "Buddha", "Love", "Spider", "Sound", "Phoenix", "Portal", "Lightning", "Pain", "Blizzard", "Gravity", "Mammoth", "T-Rex", "Dragon", "Tiger", "Kitsune", "Dough", "Shadow", "Venom", "Spirit", "Control"}

-- 1. Finds random targets
local targets = {}
for _, p in pairs(Players:GetPlayers()) do
    if p ~= player then table.insert(targets, p.Name) end
end

-- 2. Spam chat with fake gifts (RANDOM FRUIT)
while true do
    local target = targets[math.random(1, #targets)]
    local fruit = fruits[math.random(1, #fruits)]  -- PICK RANDOM FRUIT
    
    -- Fake gift messages (everyone sees)
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(
        player.Name .. " gifted Permanent " .. fruit .. " to " .. target .. " (25k Robux) 💎", "All"
    )
    
    -- Cooldown fake
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(
        player.Name .. " 2h cooldown", "All"
    )
    
    wait(5)  -- Spam delay
end
